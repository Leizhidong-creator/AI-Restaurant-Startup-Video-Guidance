# Security

请不要在 Issue、Pull Request 或日志中提交 API key、访问令牌、密码、私钥、Cookie 或包含凭据的配置文件。

如果发现疑似已泄露的凭据：

1. 立即在对应服务商处撤销并轮换凭据。
2. 检查服务商审计日志和账单，确认是否被滥用。
3. 使用 `git filter-repo` 或 BFG 从所有 Git 引用中清理旧值，然后强制更新远端历史。
4. 清理本地克隆、构建产物和 CI 缓存中的副本。

安全问题请通过仓库维护者的私下渠道报告，不要公开粘贴敏感值。


